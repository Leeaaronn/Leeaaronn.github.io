---
phase: 04-hero-section
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - src/style.css
autonomous: false
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - HERO-04
  - HERO-05
  - HERO-06

must_haves:
  truths:
    - "Eyebrow text 'Data Analyst . Los Angeles, CA' is visible in mono font above the title"
    - "Title 'Data that tells the real story' is visible with 'real story' in italic accent color"
    - "Subtitle sentence about tested pipelines is visible below the title"
    - "Three CTA buttons (GitHub, LinkedIn, Email Me) are visible and clickable"
    - "Scroll indicator with 'Scroll to explore' text and animated line appears at bottom of hero"
    - "All hero elements fade in with staggered timing on page load"
  artifacts:
    - path: "index.html"
      provides: "Hero section HTML content"
      contains: "hero__eyebrow"
    - path: "src/style.css"
      provides: "Hero styles and fade-in animation"
      contains: "hero-fade-in"
  key_links:
    - from: "index.html"
      to: "src/style.css"
      via: "hero__ BEM class names"
      pattern: "hero__eyebrow|hero__title|hero__cta"
---

<objective>
Add hero section content, styling, and staggered load animation to the portfolio site.

Purpose: The hero is the first thing visitors see — it must communicate Aaron's name, role, value proposition, and provide clear CTAs. The globe from Phase 2 remains visible behind the content.
Output: Fully styled hero section with eyebrow, title, subtitle, CTA buttons, scroll indicator, and staggered CSS fade-in animation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-hero-section/04-CONTEXT.md

<interfaces>
<!-- Existing HTML structure — hero section is an empty shell at line 49-51 of index.html -->
From index.html:
```html
<section id="hero" class="section">
  <!-- Hero content — Phase 4 -->
</section>
```

<!-- Existing CSS design tokens from src/style.css -->
From src/style.css:
```css
:root {
  --color-black:    #000000;
  --color-dark-1:   #0a0a0a;
  --color-white:    #ffffff;
  --color-grey-1:   #cccccc;
  --color-grey-2:   #999999;
  --accent:         #60a5fa;
  --font-display:  'Instrument Serif', Georgia, serif;
  --font-body:     'Syne', system-ui, sans-serif;
  --font-mono:     'JetBrains Mono', 'Courier New', monospace;
  --text-hero:     clamp(3.75rem, 8vw, 7.5rem); /* 60px-120px */
  --transition-fast:   150ms ease;
  --transition-base:   300ms ease;
}
```

<!-- .section base styles already applied -->
```css
.section {
  min-height: 100vh;
  min-height: 100svh;
  scroll-snap-align: start;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hero HTML content to index.html</name>
  <files>index.html</files>
  <read_first>
    - index.html (current shell — hero section at lines 49-51)
    - .planning/phases/04-hero-section/04-CONTEXT.md (exact text content and link decisions)
  </read_first>
  <action>
Replace the hero section placeholder comment (line 50: `<!-- Hero content — Phase 4 -->`) with the full hero content structure inside `<section id="hero" class="section">`:

```html
<div class="hero__content">
  <p class="hero__eyebrow">Data Analyst &middot; Los Angeles, CA</p>
  <h1 class="hero__title">Data that tells the <em>real story</em></h1>
  <p class="hero__subtitle">I build tested pipelines, causal analyses, and dashboards that turn messy data into clear, actionable insight.</p>
  <div class="hero__ctas">
    <a href="https://github.com/Leeaaronn" target="_blank" rel="noopener noreferrer" class="hero__cta">GitHub</a>
    <a href="https://www.linkedin.com/in/aaronlee-data" target="_blank" rel="noopener noreferrer" class="hero__cta">LinkedIn</a>
    <a href="mailto:aaronlee.data@gmail.com" class="hero__cta">Email Me</a>
  </div>
  <div class="hero__scroll-cue">
    <span class="hero__scroll-text">Scroll to explore</span>
    <span class="hero__scroll-line"></span>
  </div>
</div>
```

Notes on the HTML structure:
- `hero__content` is the wrapper div for z-index layering above the globe canvas
- `hero__eyebrow` is a `<p>` tag with the mono-font role line (HERO-01)
- `hero__title` uses `<h1>` with `<em>` wrapping "real story" for semantic italic + accent color (HERO-02)
- `hero__subtitle` is the value proposition sentence (HERO-03)
- `hero__ctas` contains three `<a>` tags styled as buttons (HERO-04)
- `hero__scroll-cue` at the bottom holds the scroll text and animated line (HERO-05)
- GitHub URL: `https://github.com/Leeaaronn` (Aaron's actual GitHub username from the repo)
- LinkedIn URL: `https://www.linkedin.com/in/aaronlee-data` (placeholder — Claude's discretion per CONTEXT.md)
- Email: `mailto:aaronlee.data@gmail.com` (placeholder — Claude's discretion per CONTEXT.md)

Do NOT modify anything outside the `<section id="hero">` element. Keep all other HTML intact.
  </action>
  <verify>
    <automated>cd C:/Users/leeaa/Leeaaronn.github.io && grep -c "hero__eyebrow" index.html && grep -c "hero__title" index.html && grep -c "hero__cta" index.html && grep -c "hero__scroll-cue" index.html && grep "real story" index.html</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `class="hero__eyebrow"` exactly once
    - index.html contains `class="hero__title"` exactly once
    - index.html contains `class="hero__subtitle"` exactly once
    - index.html contains `class="hero__cta"` exactly 3 times (GitHub, LinkedIn, Email Me)
    - index.html contains `class="hero__scroll-cue"` exactly once
    - index.html contains `<em>real story</em>` inside the h1
    - index.html contains `Data Analyst` in the eyebrow paragraph
    - index.html contains `href="https://github.com/Leeaaronn"`
    - index.html contains `href="mailto:` for the Email Me link
    - The `<section id="hero" class="section">` tag is unchanged
    - No other sections are modified
  </acceptance_criteria>
  <done>Hero section in index.html contains eyebrow, title with italic accent "real story", subtitle, 3 CTA links, and scroll indicator — all with hero__ BEM class names</done>
</task>

<task type="auto">
  <name>Task 2: Add hero CSS styles and staggered fade-in animation</name>
  <files>src/style.css</files>
  <read_first>
    - src/style.css (current styles — add new section after Section 7 or Section 8)
    - .planning/phases/04-hero-section/04-CONTEXT.md (exact font sizes, colors, spacing, animation timing)
    - index.html (confirm class names from Task 1)
  </read_first>
  <action>
Add a new CSS section after the existing "Section 8 — Scroll Shell UI" block in src/style.css. Use the section comment pattern consistent with the file:

```css
/* ==========================================================================
   Section 9 — Hero Section
   ========================================================================== */

/* --- Hero Layout --- */
.hero__content {
  position: relative;
  z-index: 10;
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  max-width: 900px;
  padding: 0 var(--space-8);
}

/* --- Eyebrow (HERO-01) --- */
.hero__eyebrow {
  font-family: var(--font-mono);
  font-size: 11px;
  text-transform: uppercase;
  letter-spacing: 4px;
  color: var(--color-grey-2);
  margin-bottom: var(--space-6);
}

/* --- Title (HERO-02) --- */
.hero__title {
  font-family: var(--font-display);
  font-size: clamp(52px, 9vw, 120px);
  font-weight: 400;
  line-height: 1.05;
  color: var(--color-white);
  margin-bottom: var(--space-6);
}

.hero__title em {
  font-style: italic;
  color: var(--accent);
}

/* --- Subtitle (HERO-03) --- */
.hero__subtitle {
  font-family: var(--font-body);
  font-size: 17px;
  color: #777777;
  line-height: 1.6;
  max-width: 600px;
  margin-bottom: var(--space-12);
}

/* --- CTA Buttons (HERO-04) --- */
.hero__ctas {
  display: flex;
  gap: var(--space-4);
  flex-wrap: wrap;
  justify-content: center;
  margin-bottom: var(--space-16);
}

.hero__cta {
  font-family: var(--font-mono);
  font-size: 12px;
  color: var(--color-grey-1);
  border: 1px solid #333333;
  border-radius: 8px;
  padding: 14px 28px;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  transition: border-color var(--transition-fast), color var(--transition-fast);
}

.hero__cta:hover {
  border-color: var(--accent);
  color: var(--color-white);
}

/* --- Scroll Indicator (HERO-05) --- */
.hero__scroll-cue {
  position: absolute;
  bottom: var(--space-12);
  left: 50%;
  transform: translateX(-50%);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: var(--space-2);
}

.hero__scroll-text {
  font-family: var(--font-mono);
  font-size: 10px;
  text-transform: uppercase;
  letter-spacing: 2px;
  color: var(--color-grey-2);
}

.hero__scroll-line {
  width: 1px;
  height: 40px;
  background: var(--color-grey-2);
  animation: scroll-line-pulse 2s ease-in-out infinite;
}

@keyframes scroll-line-pulse {
  0%, 100% { opacity: 0.3; transform: scaleY(0.6); }
  50% { opacity: 1; transform: scaleY(1); }
}

/* --- Staggered Fade-in Animation (HERO-06) --- */
@keyframes hero-fade-in {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.hero__eyebrow,
.hero__title,
.hero__subtitle,
.hero__ctas,
.hero__scroll-cue {
  opacity: 0;
  animation: hero-fade-in 0.8s ease-out forwards;
}

.hero__eyebrow    { animation-delay: 0.5s; }
.hero__title      { animation-delay: 0.8s; }
.hero__subtitle   { animation-delay: 1.1s; }
.hero__ctas       { animation-delay: 1.3s; }
.hero__scroll-cue { animation-delay: 1.5s; }

/* --- Hero Mobile --- */
@media (max-width: 768px) {
  .hero__content {
    padding: 0 var(--space-4);
  }
  .hero__ctas {
    flex-direction: column;
    align-items: center;
  }
  .hero__cta {
    width: 100%;
    max-width: 280px;
    text-align: center;
  }
}
```

Key implementation details:
- `z-index: 10` on `.hero__content` places hero text above the globe canvas (globe canvas is z-index from Three.js, lower)
- Title font size uses `clamp(52px, 9vw, 120px)` per CONTEXT.md decision, NOT the existing `--text-hero` token (which is `clamp(3.75rem, 8vw, 7.5rem)`) — the CONTEXT.md decision is the locked specification
- Subtitle color is `#777777` (inline hex) since it's between `--color-grey-1` (#ccc) and `--color-grey-2` (#999) and not a design token — per CONTEXT.md Claude's discretion note
- All hero children start at `opacity: 0` and animate to `opacity: 1` via `hero-fade-in` keyframes with staggered `animation-delay` values matching CONTEXT.md exactly: 0.5s, 0.8s, 1.1s, 1.3s, 1.5s
- Animation uses `forwards` fill mode so elements remain visible after animation completes
- Scroll indicator line uses a separate `scroll-line-pulse` keyframe for the infinite pulsing effect
- Mobile breakpoint at 768px stacks CTA buttons vertically

Do NOT modify any existing CSS. Only append the new Section 9 block.
  </action>
  <verify>
    <automated>cd C:/Users/leeaa/Leeaaronn.github.io && grep -c "hero-fade-in" src/style.css && grep -c "hero__content" src/style.css && grep -c "hero__cta" src/style.css && grep "animation-delay: 0.5s" src/style.css && grep "animation-delay: 1.5s" src/style.css && grep "scroll-line-pulse" src/style.css && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/style.css contains `@keyframes hero-fade-in` exactly once
    - src/style.css contains `@keyframes scroll-line-pulse` exactly once
    - src/style.css contains `.hero__content` with `z-index: 10`
    - src/style.css contains `.hero__eyebrow` with `font-size: 11px`
    - src/style.css contains `.hero__title` with `clamp(52px, 9vw, 120px)`
    - src/style.css contains `.hero__title em` with `color: var(--accent)`
    - src/style.css contains `.hero__subtitle` with `color: #777777`
    - src/style.css contains `.hero__cta` with `border: 1px solid #333333` and `border-radius: 8px` and `padding: 14px 28px`
    - src/style.css contains `.hero__cta:hover` with `border-color: var(--accent)`
    - src/style.css contains `.hero__scroll-text` with `font-size: 10px`
    - Five animation-delay values present: 0.5s, 0.8s, 1.1s, 1.3s, 1.5s
    - src/style.css contains `Section 9` section comment
    - `npm run build` exits with code 0
    - No existing CSS sections (1-8) are modified
  </acceptance_criteria>
  <done>Hero section has complete CSS: layout with z-index above globe, eyebrow/title/subtitle/CTA/scroll-cue typography, hover states on CTAs, scroll line animation, staggered fade-in with 5 delay steps, and mobile responsive CTA stacking</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of hero section</name>
  <files>index.html, src/style.css</files>
  <action>
Human verifies the completed hero section visually. Run `npm run dev` and open http://localhost:5173 to inspect:
1. Staggered fade-in: eyebrow appears first, then title, subtitle, CTAs, and scroll indicator in sequence over ~2 seconds
2. Eyebrow reads "DATA ANALYST . LOS ANGELES, CA" in small mono font
3. Title reads "Data that tells the real story" with "real story" in italic blue (#60a5fa)
4. Subtitle text is visible in a slightly dimmer color below the title
5. Three CTA buttons visible: GitHub, LinkedIn, Email Me
6. Hover each CTA button — border should turn blue (#60a5fa)
7. Click GitHub — opens Aaron's GitHub profile in new tab
8. Click Email Me — opens email client
9. "Scroll to explore" text and pulsing line at bottom of hero
10. Globe/stars are visible behind the hero text
11. Resize to mobile width — CTAs stack vertically
  </action>
  <verify>
    <automated>cd C:/Users/leeaa/Leeaaronn.github.io && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>User has visually confirmed hero section renders correctly — all content visible, animations play, CTAs work, globe visible behind content</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `npm run dev` shows the hero section with all content visible
- All 6 HERO requirements (01-06) are addressed by the HTML and CSS
- Globe from Phase 2 remains visible behind hero content (z-index layering)
- No existing Phase 1-3 styles or HTML are broken
</verification>

<success_criteria>
- Hero eyebrow, title, subtitle, CTAs, and scroll indicator are all visible on the hero section
- "real story" renders in italic accent blue (#60a5fa)
- CTA buttons have border hover effect transitioning to accent blue
- Staggered fade-in plays on page load with 5 sequential delays
- Scroll indicator line pulses with infinite animation
- Globe and stars remain visible behind hero content
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-hero-section/04-01-SUMMARY.md`
</output>
