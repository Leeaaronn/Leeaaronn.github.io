---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.js
  - src/main.js
  - src/globe.js
  - src/stars.js
  - src/scroll.js
autonomous: true
requirements:
  - SCAF-01

must_haves:
  truths:
    - "npm run dev starts the Vite dev server without errors"
    - "npm run build produces a dist/ directory without errors"
    - "src/main.js, src/globe.js, src/stars.js, src/scroll.js all exist as ES modules"
    - "vite.config.js sets base to '/'"
  artifacts:
    - path: "package.json"
      provides: "Project metadata, scripts, three dependency"
      contains: "\"three\""
    - path: "vite.config.js"
      provides: "Vite configuration with correct base path"
      contains: "base: '/'"
    - path: "src/main.js"
      provides: "Entry point that imports CSS and stub modules"
    - path: "src/globe.js"
      provides: "Stub ES module export for Phase 2"
    - path: "src/stars.js"
      provides: "Stub ES module export for Phase 2"
    - path: "src/scroll.js"
      provides: "Stub ES module export for Phase 2"
  key_links:
    - from: "src/main.js"
      to: "src/style.css"
      via: "import './style.css'"
      pattern: "import.*style\\.css"
    - from: "src/main.js"
      to: "src/globe.js"
      via: "import { initGlobe } from './globe.js'"
      pattern: "import.*globe"
---

<objective>
Initialize the Vite + vanilla JS project scaffold with the correct file structure, dependencies, and build configuration.

Purpose: Establishes the build foundation all subsequent phases depend on. Nothing else can run until `npm run dev` works.
Output: package.json, vite.config.js, and four JS module files (main.js + three stubs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Vite scaffold and install dependencies</name>
  <files>package.json, vite.config.js</files>
  <read_first>
    - CLAUDE.md (§Technology Stack — Vite 8 config, GitHub Pages base path, package.json scripts)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-01 file structure, canonical refs)
  </read_first>
  <action>
    Run these commands from the project root to scaffold the project:

    1. Initialize package.json manually (do NOT use `npm create vite@latest` which would create a subdirectory):

    Create `package.json`:
    ```json
    {
      "name": "leeaaronn-portfolio",
      "private": true,
      "version": "0.0.0",
      "type": "module",
      "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview"
      },
      "dependencies": {
        "three": "^0.183.0"
      },
      "devDependencies": {
        "vite": "^8.0.3"
      }
    }
    ```

    2. Run `npm install` to install dependencies.

    3. Create `vite.config.js`:
    ```js
    import { defineConfig } from 'vite'

    export default defineConfig({
      base: '/',
    })
    ```

    NOTE: base is '/' (not '/Leeaaronn.github.io/') because this is a user page (leeaaronn.github.io), not a project page. User pages use root base.
  </action>
  <verify>
    <automated>cd /c/Users/leeaa/Leeaaronn.github.io && cat package.json | grep '"three"' && cat vite.config.js | grep "base: '/'"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"three": "^0.183.0"` under dependencies
    - `package.json` contains `"vite": "^8.0.3"` under devDependencies
    - `package.json` has `"type": "module"`
    - `package.json` scripts include `"dev": "vite"`, `"build": "vite build"`, `"preview": "vite preview"`
    - `vite.config.js` contains `base: '/'`
    - `node_modules/` directory exists (npm install ran successfully)
    - `node_modules/three/` directory exists
  </acceptance_criteria>
  <done>package.json and vite.config.js written, npm install completed, three and vite in node_modules</done>
</task>

<task type="auto">
  <name>Task 2: Create src/ entry point and module stubs</name>
  <files>src/main.js, src/globe.js, src/stars.js, src/scroll.js</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (D-01 file structure, D-02 assets path, integration points)
    - .planning/PROJECT.md (design constraints, known issues from prototype)
  </read_first>
  <action>
    Create the `src/` directory with the entry point and three stub modules.

    **src/main.js** — Entry point, imports CSS and stubs:
    ```js
    import './style.css'
    import { initGlobe } from './globe.js'
    import { initStars } from './stars.js'
    import { initScroll } from './scroll.js'

    // Phase 2: initialize globe and stars when DOM is ready
    // Phase 3: initialize scroll behavior
    // Stubs export no-op functions until their phases are implemented
    ```

    **src/globe.js** — Stub for Phase 2:
    ```js
    /**
     * Globe module — Three.js Earth globe with NASA Blue Marble texture.
     * Full implementation in Phase 2 (Canvas Layer).
     */
    export function initGlobe() {
      // Phase 2 implementation
    }
    ```

    **src/stars.js** — Stub for Phase 2:
    ```js
    /**
     * Stars module — fixed-position Three.js star field with drift animation.
     * Full implementation in Phase 2 (Canvas Layer).
     */
    export function initStars() {
      // Phase 2 implementation
    }
    ```

    **src/scroll.js** — Stub for Phase 3:
    ```js
    /**
     * Scroll module — section detection, progress bar, dot navigation.
     * Full implementation in Phase 3 (Scroll Shell).
     */
    export function initScroll() {
      // Phase 3 implementation
    }
    ```

    NOTE: Do NOT call `initGlobe()`, `initStars()`, or `initScroll()` yet in main.js — they are stubs. Leave the calls commented out to avoid errors during Phase 1 validation.
  </action>
  <verify>
    <automated>cd /c/Users/leeaa/Leeaaronn.github.io && ls src/ && grep "initGlobe" src/globe.js && grep "initStars" src/stars.js && grep "initScroll" src/scroll.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/main.js` exists and contains `import './style.css'`
    - `src/main.js` imports from `'./globe.js'`, `'./stars.js'`, `'./scroll.js'`
    - `src/globe.js` exports function `initGlobe`
    - `src/stars.js` exports function `initStars`
    - `src/scroll.js` exports function `initScroll`
    - Running `npm run build` in project root exits 0 (no errors)
  </acceptance_criteria>
  <done>All four src/ files created with correct exports. npm run build exits 0.</done>
</task>

</tasks>

<verification>
From project root:
1. `npm run dev` starts without error (check terminal for "Local: http://localhost:5173/")
2. `npm run build` exits 0 and creates `dist/` directory
3. `cat package.json | grep '"three"'` outputs the three dependency line
4. `cat vite.config.js | grep "base: '/'"` outputs the base line
5. `ls src/` shows main.js, globe.js, stars.js, scroll.js, style.css (style.css created in Plan 02)
</verification>

<success_criteria>
- npm run dev and npm run build work without errors
- package.json declares three ^0.183.0 and vite ^8.0.3
- vite.config.js sets base: '/'
- src/main.js imports CSS and all three stub modules
- src/globe.js, src/stars.js, src/scroll.js each export their init function
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` using the summary template.
</output>
