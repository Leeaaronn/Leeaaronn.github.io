---
phase: 05-about-section
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - src/style.css
  - src/scroll.js
autonomous: false
requirements:
  - ABOU-01
  - ABOU-02
  - ABOU-03
  - ABOU-04
  - ABOU-05
  - ABOU-06
  - RESP-01

must_haves:
  truths:
    - "Portrait loads from ./assets/portrait.jpg with rounded corners, hover scale, and 'Rosemead, CA' label"
    - "'Hi, I'm Aaron.' heading is visible with 'Aaron.' in accent blue #60a5fa"
    - "Stats row shows 'B.S. CS CSUF '25 | 4 Projects | 44 Pytest Assertions | +38.7K Riders/Day'"
    - "Detail chips for Education, Focus, Goal, Next Step are visible"
    - "Resume download button links to ./assets/resume.pdf and opens in new tab"
    - "About section content fades up when the section snaps into view"
    - "On mobile, portrait stacks above text (not side-by-side)"
  artifacts:
    - path: "index.html"
      provides: "About section HTML content"
      contains: "about__portrait"
    - path: "src/style.css"
      provides: "About section styles, fade-up animation, responsive layout"
      contains: "about__content"
    - path: "src/scroll.js"
      provides: "IntersectionObserver for about fade-up trigger"
      contains: "about-visible"
  key_links:
    - from: "index.html"
      to: "src/style.css"
      via: "about__ BEM class names"
      pattern: "about__portrait|about__heading|about__stats|about__chips"
    - from: "src/scroll.js"
      to: "index.html"
      via: "about-visible class toggle on #about section"
---

<objective>
Add about section content, styling, fade-up animation, and mobile responsive layout.

Purpose: The about section introduces Aaron personally — portrait, bio, stats, detail chips, and resume download. This is the final content phase that makes the site shippable for job applications.
Output: Fully styled about section with two-column desktop layout (portrait left, text right), fade-up on scroll-snap, and mobile stacking.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing HTML — about section is empty at line 66-68 of index.html -->
From index.html:
```html
<section id="about" class="section">
  <!-- About content — Phase 5 -->
</section>
```

<!-- Existing CSS design tokens -->
From src/style.css:
```css
:root {
  --color-black:    #000000;
  --color-dark-1:   #0a0a0a;
  --color-dark-2:   #111111;
  --color-dark-3:   #1a1a1a;
  --color-white:    #ffffff;
  --color-grey-1:   #cccccc;
  --color-grey-2:   #999999;
  --accent:         #60a5fa;
  --font-display:  'Instrument Serif', Georgia, serif;
  --font-body:     'Syne', system-ui, sans-serif;
  --font-mono:     'JetBrains Mono', 'Courier New', monospace;
}
```

<!-- Scroll module — need to add IntersectionObserver for about fade-up -->
From src/scroll.js:
- initScroll() already runs on DOMContentLoaded
- Can add IntersectionObserver inside initScroll or as separate logic in main.js
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add about section HTML content to index.html</name>
  <files>index.html</files>
  <read_first>
    - index.html (current about section at lines 66-68)
    - src/style.css (existing design tokens for reference)
  </read_first>
  <action>
Replace the about section placeholder comment (line 67: `<!-- About content — Phase 5 -->`) with the full about content structure inside `<section id="about" class="section">`:

```html
<div class="about__content">
  <!-- Left: Portrait -->
  <div class="about__portrait-wrap">
    <img src="./assets/portrait.jpg" alt="Aaron Lee" class="about__portrait" />
    <span class="about__portrait-label">Rosemead, CA</span>
  </div>

  <!-- Right: Bio + Stats + Chips + Resume -->
  <div class="about__text">
    <h2 class="about__heading">Hi, I'm <span class="about__name">Aaron.</span></h2>

    <p class="about__bio">I'm a recent Computer Science graduate from Cal State Fullerton with a passion for turning raw data into clear, actionable insight. I specialize in building tested data pipelines, causal analyses, and dashboards that help teams make better decisions.</p>

    <p class="about__bio">When I'm not wrangling datasets, I'm exploring new visualization techniques and finding stories hidden in the numbers.</p>

    <div class="about__stats">
      <div class="about__stat">
        <span class="about__stat-value">B.S. CS</span>
        <span class="about__stat-label">CSUF '25</span>
      </div>
      <div class="about__stat">
        <span class="about__stat-value">4</span>
        <span class="about__stat-label">Projects</span>
      </div>
      <div class="about__stat">
        <span class="about__stat-value">44</span>
        <span class="about__stat-label">Pytest Assertions</span>
      </div>
      <div class="about__stat">
        <span class="about__stat-value">+38.7K</span>
        <span class="about__stat-label">Riders/Day</span>
      </div>
    </div>

    <div class="about__chips">
      <span class="about__chip">
        <span class="about__chip-label">Education</span>
        <span class="about__chip-value">B.S. Computer Science, CSUF</span>
      </span>
      <span class="about__chip">
        <span class="about__chip-label">Focus</span>
        <span class="about__chip-value">Data Analytics & Operations</span>
      </span>
      <span class="about__chip">
        <span class="about__chip-label">Goal</span>
        <span class="about__chip-value">Entry-level Data Analyst</span>
      </span>
      <span class="about__chip">
        <span class="about__chip-label">Next Step</span>
        <span class="about__chip-value">Full-time position in LA area</span>
      </span>
    </div>

    <a href="./assets/resume.pdf" download target="_blank" rel="noopener noreferrer" class="about__resume-btn">
      Download Resume
    </a>
  </div>
</div>
```

Structure notes:
- `about__content` is the two-column flex wrapper
- `about__portrait-wrap` holds the image + overlay label (position: relative)
- `about__portrait` is the `<img>` tag loading from `./assets/portrait.jpg`
- `about__portrait-label` is absolutely positioned at bottom-left of the image
- `about__name` wraps "Aaron." for accent color styling
- Two `about__bio` paragraphs in Syne 16px
- `about__stats` is a flex row with 4 stat items separated by dividers
- `about__chips` has 4 chip elements with label + value
- `about__resume-btn` is a styled link to ./assets/resume.pdf opening in new tab

Do NOT modify anything outside the `<section id="about">` element.
  </action>
  <verify>
    <automated>cd C:/Users/leeaa/Leeaaronn.github.io && grep -c "about__portrait" index.html && grep -c "about__heading" index.html && grep -c "about__stat" index.html && grep -c "about__chip" index.html && grep -c "about__resume-btn" index.html && grep "portrait.jpg" index.html && grep "resume.pdf" index.html</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `class="about__content"` exactly once
    - index.html contains `class="about__portrait"` exactly once
    - index.html contains `class="about__portrait-label"` exactly once with text "Rosemead, CA"
    - index.html contains `class="about__heading"` exactly once
    - index.html contains `class="about__name"` exactly once with text "Aaron."
    - index.html contains `class="about__bio"` exactly 2 times
    - index.html contains `class="about__stat"` at least 4 times (4 stat items, each with sub-elements)
    - index.html contains `class="about__chip"` at least 4 times
    - index.html contains `class="about__resume-btn"` exactly once
    - index.html contains `src="./assets/portrait.jpg"` exactly once
    - index.html contains `href="./assets/resume.pdf"` with `download` attribute and `target="_blank"`
    - index.html contains text "B.S. CS" and "CSUF '25"
    - index.html contains text "44" and "Pytest Assertions"
    - index.html contains text "+38.7K" and "Riders/Day"
    - The `<section id="about" class="section">` tag is unchanged
    - No other sections are modified
  </acceptance_criteria>
  <done>About section HTML complete with portrait, bio, stats, chips, and resume download button</done>
</task>

<task type="auto">
  <name>Task 2: Add about section CSS styles and fade-up animation</name>
  <files>src/style.css</files>
  <read_first>
    - src/style.css (add new section after Section 9 — Hero Section)
    - index.html (confirm class names from Task 1)
  </read_first>
  <action>
Append a new CSS section after the existing "Section 9 — Hero Section" block (after the hero mobile media query closing brace) in src/style.css:

```css
/* ==========================================================================
   Section 10 — About Section
   ========================================================================== */

/* --- About Layout (two-column) --- */
.about__content {
  position: relative;
  z-index: 10;
  display: flex;
  align-items: flex-start;
  gap: var(--space-16);
  max-width: 1000px;
  padding: 0 var(--space-8);
  width: 100%;
}

/* --- Fade-up animation (ABOU-06) --- */
@keyframes about-fade-up {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

#about .about__content {
  opacity: 0;
  transition: none;
}

#about.about-visible .about__content {
  animation: about-fade-up 0.8s ease-out forwards;
}

/* --- Portrait (ABOU-01) --- */
.about__portrait-wrap {
  position: relative;
  flex-shrink: 0;
}

.about__portrait {
  width: 300px;
  border-radius: 16px;
  border: 1px solid rgba(255, 255, 255, 0.1);
  transition: transform var(--transition-base);
}

.about__portrait:hover {
  transform: scale(1.04);
}

.about__portrait-label {
  position: absolute;
  bottom: 12px;
  left: 12px;
  font-family: var(--font-mono);
  font-size: 10px;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: var(--color-white);
  background: rgba(0, 0, 0, 0.6);
  padding: 4px 8px;
  border-radius: 4px;
}

/* --- Heading (ABOU-02) --- */
.about__heading {
  font-family: var(--font-display);
  font-size: 46px;
  font-weight: 400;
  line-height: 1.2;
  color: var(--color-white);
  margin-bottom: var(--space-6);
}

.about__name {
  color: var(--accent);
}

/* --- Bio paragraphs --- */
.about__bio {
  font-family: var(--font-body);
  font-size: 16px;
  color: #aaaaaa;
  line-height: 1.7;
  margin-bottom: var(--space-4);
}

/* --- Stats Row (ABOU-03) --- */
.about__stats {
  display: flex;
  gap: var(--space-8);
  margin-top: var(--space-8);
  margin-bottom: var(--space-8);
  padding: var(--space-6) 0;
  border-top: 1px solid rgba(255, 255, 255, 0.08);
  border-bottom: 1px solid rgba(255, 255, 255, 0.08);
}

.about__stat {
  display: flex;
  flex-direction: column;
  gap: var(--space-1);
}

.about__stat-value {
  font-family: var(--font-mono);
  font-size: var(--text-xl);
  font-weight: 500;
  color: var(--color-white);
}

.about__stat-label {
  font-family: var(--font-body);
  font-size: 12px;
  color: var(--color-grey-2);
  text-transform: uppercase;
  letter-spacing: 0.08em;
}

/* --- Detail Chips (ABOU-04) --- */
.about__chips {
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-3);
  margin-bottom: var(--space-8);
}

.about__chip {
  display: inline-flex;
  align-items: center;
  gap: var(--space-2);
  background: var(--color-dark-2);
  border: 1px solid rgba(255, 255, 255, 0.06);
  border-radius: 20px;
  padding: 6px 14px;
}

.about__chip-label {
  font-family: var(--font-mono);
  font-size: 10px;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--accent);
}

.about__chip-value {
  font-family: var(--font-body);
  font-size: 13px;
  color: var(--color-grey-1);
}

/* --- Resume Button (ABOU-05) --- */
.about__resume-btn {
  display: inline-block;
  font-family: var(--font-mono);
  font-size: 12px;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: var(--accent);
  border: 1px solid var(--accent);
  border-radius: 8px;
  padding: 14px 28px;
  transition: background var(--transition-fast), color var(--transition-fast);
}

.about__resume-btn:hover {
  background: var(--accent);
  color: var(--color-black);
}

/* --- About Mobile (RESP-01) --- */
@media (max-width: 768px) {
  .about__content {
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: var(--space-8);
    padding: 0 var(--space-4);
  }

  .about__portrait {
    width: 240px;
  }

  .about__stats {
    flex-wrap: wrap;
    gap: var(--space-4);
    justify-content: center;
  }

  .about__stat {
    align-items: center;
  }

  .about__chips {
    justify-content: center;
  }

  .about__resume-btn {
    width: 100%;
    max-width: 280px;
    text-align: center;
  }
}
```

Key implementation details:
- `z-index: 10` on `.about__content` layers text above the globe canvas (consistent with hero)
- Portrait: 300px width, border-radius 16px, 1px solid rgba(255,255,255,0.1) border, hover scale(1.04)
- Portrait label: JetBrains Mono 10px at bottom-left with dark background overlay
- Heading: Instrument Serif 46px, "Aaron." in --accent color
- Bio: Syne 15px, color #aaaaaa
- Stats row separated by top/bottom 1px rgba borders
- Chips: pill-shaped with dark-2 background, mono label in accent, body value in grey-1
- Resume button: mono font, accent border, fills accent on hover
- Fade-up: `about-fade-up` keyframe triggered by `about-visible` class on `#about`
- Mobile 768px: flex-direction column, portrait stacks above text, centered layout

Do NOT modify any existing CSS sections (1-9).
  </action>
  <verify>
    <automated>cd C:/Users/leeaa/Leeaaronn.github.io && grep -c "about-fade-up" src/style.css && grep -c "about__portrait" src/style.css && grep -c "about__stats" src/style.css && grep -c "about__chip" src/style.css && grep -c "about__resume-btn" src/style.css && grep "about-visible" src/style.css && grep "flex-direction: column" src/style.css && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/style.css contains `@keyframes about-fade-up` exactly once
    - src/style.css contains `.about__content` with `z-index: 10`
    - src/style.css contains `.about__portrait` with `width: 300px` and `border-radius: 16px`
    - src/style.css contains `.about__portrait` with `border: 1px solid rgba(255, 255, 255, 0.1)`
    - src/style.css contains `.about__portrait:hover` with `transform: scale(1.04)`
    - src/style.css contains `.about__portrait-label` with `font-size: 10px` and `font-family: var(--font-mono)`
    - src/style.css contains `.about__heading` with `font-size: 46px` and `font-family: var(--font-display)`
    - src/style.css contains `.about__name` with `color: var(--accent)`
    - src/style.css contains `.about__bio` with `font-size: 16px` and `color: #aaaaaa`
    - src/style.css contains `.about__stats` flex row with gap
    - src/style.css contains `.about__chip` with `border-radius: 20px`
    - src/style.css contains `.about__chip-label` with `color: var(--accent)`
    - src/style.css contains `.about__resume-btn` with `border: 1px solid var(--accent)`
    - src/style.css contains `.about__resume-btn:hover` with `background: var(--accent)`
    - src/style.css contains `#about.about-visible .about__content` triggering fade-up
    - src/style.css contains `@media (max-width: 768px)` with `.about__content` having `flex-direction: column`
    - src/style.css contains `Section 10` section comment
    - `npm run build` exits with code 0
    - No existing CSS sections (1-9) are modified
  </acceptance_criteria>
  <done>About section CSS complete: two-column layout, portrait styles, heading with accent name, bio typography, stats row, chips, resume button, fade-up animation, and mobile responsive stacking</done>
</task>

<task type="auto">
  <name>Task 3: Add IntersectionObserver for about section fade-up trigger</name>
  <files>src/scroll.js</files>
  <read_first>
    - src/scroll.js (current scroll module — add observer inside initScroll)
    - src/style.css (the about-visible class that triggers the animation)
  </read_first>
  <action>
Add an IntersectionObserver inside the `initScroll` function in src/scroll.js, after the existing dot click listener setup and before the body scroll listener. The observer watches `#about` and toggles the `about-visible` class:

After the line `});` that closes the `dots.forEach` block (around line 74), add:

```javascript
  // About section fade-up trigger (ABOU-06)
  const aboutSection = document.getElementById('about');
  if (aboutSection) {
    const aboutObserver = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            aboutSection.classList.add('about-visible');
          }
        });
      },
      { threshold: 0.3 }
    );
    aboutObserver.observe(aboutSection);
  }
```

Notes:
- threshold 0.3 means the animation triggers when 30% of the about section is visible (feels natural with scroll-snap)
- The class is only added, never removed — fade-up plays once and stays visible
- Uses `getElementById('about')` to find the section
- Placed inside `initScroll` so it runs on DOMContentLoaded along with other scroll setup

Do NOT modify any existing code in scroll.js. Only insert the new block.
  </action>
  <verify>
    <automated>cd C:/Users/leeaa/Leeaaronn.github.io && grep -c "about-visible" src/scroll.js && grep -c "IntersectionObserver" src/scroll.js && grep "threshold" src/scroll.js && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/scroll.js contains `new IntersectionObserver` exactly once
    - src/scroll.js contains `'about-visible'` class being added
    - src/scroll.js contains `threshold: 0.3`
    - src/scroll.js contains `document.getElementById('about')` 
    - src/scroll.js contains `aboutSection.classList.add('about-visible')`
    - The existing `initScroll` function signature is unchanged
    - The existing body scroll listener is unchanged
    - The existing dot click handlers are unchanged
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>IntersectionObserver added to scroll.js — toggles about-visible class on #about when 30% visible, triggering the CSS fade-up animation</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual verification of about section</name>
  <files>index.html, src/style.css, src/scroll.js</files>
  <action>
Human verifies the completed about section visually. Run `npm run dev` and open http://localhost:5173, then scroll to the about section:

1. **Fade-up**: About section content fades up smoothly when snapping into view
2. **Portrait**: Image loads from ./assets/portrait.jpg with rounded corners (16px radius) and subtle border
3. **Portrait hover**: Hovering the portrait scales it up slightly (1.04x)
4. **Portrait label**: "Rosemead, CA" label visible at bottom-left of portrait in small mono font
5. **Heading**: "Hi, I'm Aaron." with "Aaron." in accent blue (#60a5fa), Instrument Serif 46px
6. **Bio**: Two paragraphs of text visible in slightly muted color, readable at 15px
7. **Stats row**: Four stats visible — B.S. CS / CSUF '25, 4 / Projects, 44 / Pytest Assertions, +38.7K / Riders/Day
8. **Detail chips**: Four pill-shaped chips visible — Education, Focus, Goal, Next Step
9. **Resume button**: "Download Resume" button visible, accent blue border, fills on hover
10. **Resume link**: Clicking opens ./assets/resume.pdf in new tab
11. **Mobile**: Resize to mobile width — portrait stacks above text, content centers
12. **Globe**: Globe/stars visible behind the about section
  </action>
  <verify>
    <automated>cd C:/Users/leeaa/Leeaaronn.github.io && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>User has visually confirmed about section renders correctly — portrait, bio, stats, chips, resume button, fade-up animation, and mobile responsive layout all working</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `npm run dev` shows the about section with all content visible
- All 7 phase requirements (ABOU-01 through ABOU-06 + RESP-01) are addressed
- Globe from Phase 2 remains visible behind about content (z-index layering)
- No existing Phase 1-4 styles, HTML, or JS are broken
- Scroll-snap still works correctly through all 8 sections
</verification>

<success_criteria>
- Portrait loads from ./assets/portrait.jpg with rounded corners, hover scale, and "Rosemead, CA" label
- "Hi, I'm Aaron." heading with "Aaron." in accent blue
- Two bio paragraphs readable at 15px
- Stats row with 4 items displayed
- 4 detail chips with labeled values
- Resume download button links to ./assets/resume.pdf opening in new tab
- About content fades up when section snaps into view
- Mobile layout stacks portrait above text
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-about-section/05-01-SUMMARY.md`
</output>
